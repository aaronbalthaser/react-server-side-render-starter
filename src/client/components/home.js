import React, { Component } from 'react';

const Home = () => {
  return (
    <div>
      <div>Home Page</div>
      <button onClick={() => console.log('Hello there!')}>Press me!</button>
    </div>
  );
};

export default Home;
